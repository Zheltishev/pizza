export default async function createToken(loginText: string) {
    // const res = await fetch('http://localhost:8000/create-tokens', {
    //     method: 'POST',
    //     headers: {
    //         'Content-Type': 'application/json'
    //     },
    //     body: JSON.stringify({
    //         login: loginText
    //     })
    // })
    // .then(res => res.json())
    // .then( res => {
    //     return {
    //         status: res.status,
    //         message: res.message
    //     }
    // })

    // console.log(res)

    // return res
}